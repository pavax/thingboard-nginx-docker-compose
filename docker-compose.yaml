version: '3'

services:
  webserver:
    image: nginx:latest
    ports:
      - 80:80
      - 443:443
    restart: always
    volumes:
      - nginx_conf:/etc/nginx/conf.d/:ro
      - certbot_conf:/var/www/certbot/:ro

  certbot:
    image: certbot/certbot:latest
    volumes:
      - certbot_conf:/var/www/certbot/:rw
      - certbot_conf:/etc/letsencrypt/:rw
volumes:
    nginx_conf:
    certbot_conf: